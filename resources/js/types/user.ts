export type LoginData = {
  email: string;
  password: string;
};

export type User = {
  id: string;
  name: string;
  surname: string;
  patronymic: string;
  email: string;
  avatar: string;
  birthDate: Date;
};
